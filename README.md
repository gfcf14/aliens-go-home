This is a practice project to develop games using React and Redux

## ALIENS GO HOME

Installation: Navigate to the root directory of the project, then write `npm install` on terminal

To run: Write `npm start`
